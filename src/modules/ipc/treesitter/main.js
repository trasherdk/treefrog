let Parser = require("tree-sitter");
let JavaScript = require("tree-sitter-javascript");

/*
init all parsers here - just the bare treesitter bit
*/

module.exports = {
	parse(code, lang) {
		
	},
};
